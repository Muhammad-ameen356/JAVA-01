document.write("<b>Rules of naming JS variables</b> ");

var
let = "  letters";
var num = "  numbers";
var und = " underscore";
var dol = " $";
document.write("variable means can only contain" +
    let +"," + num + "," + und + " or " + dol + "..." + "For example $ my first variable");

var alpha = "alphabates";
document.write("variable must brgin with a" +
    let +"," + alpha + "," + und + "..." + "For example $ name, _name or name");

var sens = "sensitive";
document.write(" variable names are case " + sens);

var key = "Keywords";
document.write(" variable names should not be JS " + key);